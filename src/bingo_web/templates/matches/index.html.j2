{% extends "layouts/main.html.j2" %}

{% block main %}
  <h1 class="title">Match Setup</h1>

  <div class="field is-grouped">
    <p class="control">
      <a href="/matches/new" class="button is-primary">New Match</a>
    </p>
  </div>

  <table class="table is-fullwidth">
    <thead>
      <tr>
        <th>Name</th>
        <th>Start Date</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      {% for match in matches %}
        <tr>
          <td><a href="/matches/{{match.id}}">{{ match.name }}</a></td>
          <td class="js-date-string js-localized">{{ match.start_date | strftime('%s') }}</td>
          <td>
            <a class="button is-white" href="/matches/{{match.id}}/delete">
              &#x2716;
            </a>
          </td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
{% endblock %}
